name: kind_wall
description: A pastel-themed kindness wall mobile app
publish_to: 'none' # since you’re not hosting/publishing

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Core Flutter packages
  cupertino_icons: ^1.0.2

  # Your app-specific helpers
  flutter_test:
    sdk: flutter

  # Routing & navigation
  go_router: ^13.0.0   # or whatever version you used for AppRouter

  # State management (if used)
  provider: ^6.0.5

  # Date formatting for published notes
  intl: ^0.18.0

dev_dependencies:
  flutter_lints: ^2.0.0
  test: ^1.24.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/  # folder for uplifting visuals
    - assets/icons/           # if you added pastel icons